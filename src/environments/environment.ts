// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyAEPpPWEpKQLceXZzYC7g9w9RSCsGRmFnY',
    authDomain: 'github-finder-edbc1.firebaseapp.com',
    databaseURL: 'https://github-finder-edbc1.firebaseio.com',
    projectId: 'github-finder-edbc1',
    storageBucket: 'github-finder-edbc1.appspot.com',
    messagingSenderId: '750677897901',
    appId: '1:750677897901:web:9b131ce029f33919e20c24',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
